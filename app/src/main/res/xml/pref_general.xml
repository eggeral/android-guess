<PreferenceScreen xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- NOTE: EditTextPreference accepts EditText attributes. -->
    <!-- NOTE: EditTextPreference's summary should be set to its value by the activity code. -->
    <EditTextPreference
            android:key="answer_value"
            android:title="@string/pref_title_answer"
            android:defaultValue="@string/pref_default_answer"
            android:selectAllOnFocus="true"
            android:inputType="number"
            android:capitalize="words"
            android:singleLine="true"
            android:numeric="integer"
            android:maxLines="1" />
    <ListPreference
            android:key="sort_order"
            android:title="@string/pref_sort_order"
            android:defaultValue="number"
            android:entries="@array/pref_sort_order_titles"
            android:entryValues="@array/pref_sort_order_values"
            android:negativeButtonText="@null"
            android:positiveButtonText="@null" />


</PreferenceScreen>
